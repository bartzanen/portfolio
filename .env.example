# Copy to .env.local for local development, or set in the Cloudflare Pages
# dashboard for deployed builds. Every variable here is optional — the site
# builds and runs without any of them.

# Cloudflare Web Analytics beacon token. When unset, the beacon script is not
# rendered at all, so local and preview builds stay untracked.
NEXT_PUBLIC_CF_BEACON_TOKEN=
